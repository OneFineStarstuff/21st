{
  "name": "@repo/agi-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "python3 -m unittest discover",
    "train": "python3 model.py",
    "benchmark": "python3 -m pytest benchmark.py",
    "lint": "flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics && bandit -r ."
  }
}
